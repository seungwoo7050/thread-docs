## `fix(source): scope schedule interval by publication mode`

diff --git a/grocery/migrations/0028_scope_source_schedule_interval.py b/grocery/migrations/0028_scope_source_schedule_interval.py
new file mode 100644
index 0000000..a94111f
--- /dev/null
+++ b/grocery/migrations/0028_scope_source_schedule_interval.py
@@ -0,0 +1,33 @@
+# Generated by Django 5.2.17 on 2026-08-31
+
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+    dependencies = [
+        ("grocery", "0027_allow_historical_last_known_good_rollback"),
+    ]
+
+    operations = [
+        migrations.RemoveConstraint(
+            model_name="sourceconfiguration",
+            name="grocery_source_schedule_interval_valid",
+        ),
+        migrations.AddConstraint(
+            model_name="sourceconfiguration",
+            constraint=models.CheckConstraint(
+                condition=models.Q(
+                    ("schedule_interval_hours__gt", 0),
+                    models.Q(
+                        ("publication_mode", "HISTORICAL_MONTHLY"),
+                        ("schedule_interval_hours__lte", 168),
+                    )
+                    | (
+                        ~models.Q(("publication_mode", "HISTORICAL_MONTHLY"))
+                        & models.Q(("schedule_interval_hours__lte", 24))
+                    ),
+                ),
+                name="grocery_source_schedule_interval_valid",
+            ),
+        ),
+    ]
diff --git a/grocery/models.py b/grocery/models.py
index 598a73d..ae054de 100644
--- a/grocery/models.py
+++ b/grocery/models.py
@@ -293,9 +293,18 @@ class SourceConfiguration(models.Model):
                 name="grocery_source_schedule_mode_valid",
             ),
             models.CheckConstraint(
-                condition=Q(
-                    schedule_interval_hours__gt=0,
-                    schedule_interval_hours__lte=168,
+                condition=(
+                    Q(schedule_interval_hours__gt=0)
+                    & (
+                        Q(
+                            publication_mode="HISTORICAL_MONTHLY",
+                            schedule_interval_hours__lte=168,
+                        )
+                        | (
+                            ~Q(publication_mode="HISTORICAL_MONTHLY")
+                            & Q(schedule_interval_hours__lte=24)
+                        )
+                    )
                 ),
                 name="grocery_source_schedule_interval_valid",
             ),
